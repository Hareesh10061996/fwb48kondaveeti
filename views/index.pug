extends layout

block content
  h1= title
  p Welcome to  Index.pug Assignment 10 This is Hareesh Kondaveeti

  input(type ="button",onclick="update()", class="btn btn-success", value = "Press Me")
  div(id="target") Display
  script.
      let client_count=0;
      function update()
        {
          client_count=client_count+1;
          document.getElementById("target").innerHTML = "Value now "+client_count;
        };
